[build]
  base = "/"
  command = "npm install -g pnpm && pnpm install && pnpm --filter @workspace/digital-product-passport build"
  publish = "apps/web/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
